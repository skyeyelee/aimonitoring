import type {Result,Job} from './model';
export function weeklyDelta(rows:Result[],jobs:Job[],site:string){const weekly=jobs.filter(j=>j.kind==='weekly'&&j.status==='completed').slice(0,2);if(weekly.length<2)return null;const a=rows.filter(r=>r.jobId===weekly[0].id&&r.status==='success'),b=rows.filter(r=>r.jobId===weekly[1].id&&r.status==='success');const prior=new Map(b.map(r=>[r.signature,r]));const pairs=a.filter(r=>prior.has(r.signature));if(!pairs.length)return null;const cited=(r:Result)=>site==='all'?r.sites.length>0:r.sites.includes(site);const now=pairs.filter(cited).length,prev=pairs.filter(r=>cited(prior.get(r.signature)!)).length;return {points:(now-prev)/pairs.length*100,n:pairs.length};}
