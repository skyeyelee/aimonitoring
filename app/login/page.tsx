'use client';
import {useState} from 'react';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <main style={{maxWidth:440,margin:'10vh auto',padding:28,background:'white',borderRadius:16}}><h1 style={{fontSize:25}}>밝은세상안과</h1><p>AI 모니터링 관리자 로그인</p><form className="form-stack" onSubmit={async event=>{event.preventDefault();setBusy(true);setError('');const form=new FormData(event.currentTarget);try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});const data=await r.json() as {error?:string};if(!r.ok)throw Error(data.error);location.assign('/');}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><label>이메일<input name="email" type="email" autoComplete="username" required/></label><label>비밀번호<input name="password" type="password" autoComplete="current-password" maxLength={256} required/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button primary" disabled={busy}>{busy?'확인 중…':'로그인'}</button><small>관리자가 설정한 계정으로 접속하세요. 로그인은 8시간 동안 유지됩니다.</small></form></main>}
