# 밝은세상안과 AI 모니터링 운영

이 애플리케이션은 실제 API 호출, D1 저장, URL 인용 판별을 구현합니다. 시연 데이터를 실제 측정값으로 제공하지 않습니다.

## 시작

1. 비공개 사이트에 로그인합니다. 첫 소유자 로그인은 관리자로 등록됩니다.
2. 연결·설정에서 각 공급자의 API 키와 사용 가능한 모델을 저장합니다. 키는 서버의 AES-GCM 암호화 후 저장되며 API에서 반환되지 않습니다.
3. 저장한 키의 연결을 확인합니다. 이 확인은 모델 접근 가능 여부를 검사하며 웹 검색 답변의 정상 생성까지 보장하지는 않습니다.
4. 질문·키워드와 주간 측정 국가를 검토합니다. 초기 30개 언어별 질문은 5개 의도를 6개 언어로 구성합니다. 영어 질문은 미국·캐나다·호주에서 공통으로 사용합니다. 현지어 문구는 운영팀의 추가 검수를 권장합니다.
5. 지금 측정 또는 모니터링 일정의 이번 주 정기 측정 실행을 사용합니다. 키가 없는 서비스는 건너뜁니다.

## 주간 자동화

매주 월요일 09:00 Asia/Seoul에 이 Codex 작업의 heartbeat가 비공개 대시보드를 엽니다. WebMCP가 지원되면 run_due_weekly_monitoring을 실행하고, 미지원이면 모니터링 일정의 이번 주 정기 측정 실행을 이용합니다. 저장한 활성 설정, 월 실행 한도, 국가·질문·반복 수가 적용됩니다.

자동화는 이 PC와 Codex의 실행 가능 상태 및 사이트 로그인 세션에 의존합니다. 독립적인 클라우드 cron 서비스는 아닙니다. 수동 실행은 화면을 열어둔 동안 진행합니다. 실행 슬롯과 응답을 DB에 먼저 저장하므로 중단 후 수집 계속하기로 재개할 수 있습니다. 같은 주차의 정기 측정은 중복 생성하지 않습니다. 요일·시간 변경은 자동화 예약과 애플리케이션 설정을 함께 변경해야 합니다. 현재 UI에서는 일시중지·국가·반복·한도만 변경 가능합니다.

기본 계획은 5의도 × 8국가 × 연결된 공급자 수 × 1회입니다. 3개 공급자 모두 연결 시 주 120회, 기본 월 한도 600회입니다. 재시도는 실행 한도를 사용하며 최대 3번 시도합니다. 월 한도는 금액 한도가 아닙니다. 요금은 각 공급자의 과금 정책을 따릅니다.

## 측정 의미

- OpenAI 및 Claude: 검색 위치 설정. 실제 현지 접속 IP를 확인한 결과가 아닙니다.
- Gemini: 질문 속 국가 시나리오. 실제 국가별 검색 위치 설정이 아닙니다.
- 일반 ChatGPT·Claude·Gemini 화면: 수동 관측 등록을 사용하며 자동 API 결과와 합산하지 않습니다.
- 출처 연결 정보가 없는 내용 유사성은 직접 인용으로 계산하지 않습니다.
- Gemini 출처 중계 URL은 알려진 Google 호스트에서만 목적지 헤더를 조회합니다. 목적지를 확인하지 못하면 원본 중계 URL을 보존하며 자체 사이트 인용으로 추정하지 않습니다.
- 사이트 귀속은 정규화된 정확한 호스트 기준입니다. www는 제거하고 임의 하위 도메인·도메인 간 리다이렉트는 자동 병합하지 않습니다.
- 브랜드 별칭 자동 판정은 한국어 명칭·BGSS·기본 영문 표현을 포함합니다. 다국어 병원명 판정 범위는 추가 확장이 필요합니다.

## 현재 구현 범위와 한계

완료: 6개 사이트·8개 국가, 30개 초기 질문, 3개 공급자 수집 어댑터, 인용/언급 분리, 사이트/국가/서비스/언어/방식/키워드 필터, KPI·선 그래프·히트맵·사이트 비교, 동일 조건의 주간 인용률 증감, 상세 원문·출처, 수동 관측, CSV, 주간 예약, 암호화 저장, 서버 권한 검사, 중복 방지·실패 기록·재개·실행 한도.

추후 확장: 실제 현지 IP의 사용자 화면 자동 수집, 팀원 초대 화면, 질문 문구 편집·버전 변경 UI, 월 금액 예산 관리, 방문/예약 전환 연동, 보존기간에 따른 원문 자동 정리, 대규모 데이터의 서버 집계·페이지 처리. 현재 모든 결과를 조회하므로 대규모 운영 전에 서버 집계 전환이 필요합니다. 사이트 접근은 기본 소유자 전용이며 팀 공유는 별도 설정해야 합니다.

API 키가 제공되지 않았기 때문에 실제 공급자 대상 수집 성공 및 현지 국가 지원 여부는 아직 검증되지 않았습니다. 서비스·모델에서 거절되는 위치나 검색 설정은 실패로 기록하고 인용 0회로 집계하지 않습니다.

## 검증

- TypeScript 검사, 배포 빌드.
- tests/citations.test.mjs: 도메인 위장·URL 정규화·중복·브랜드만 언급·검색 목록 제외·공급자 오류·답변 중단·Gemini 출처 매핑.
- tests/api-smoke.mjs: 로컬 인증·시드·영구 저장·키 없는 실행 차단·입력 검사·교차 출처 차단·일시중지·관측 근거 검사.
- 테스트는 유료 API 호출이나 가짜 관측 데이터를 생산 DB에 넣지 않습니다.
- 현재 브라우저 도구는 WebMCP 실행 인터페이스를 노출하지 않으므로 WebMCP 등록/실행 계약은 미검증입니다. 같은 동작을 수행하는 화면 버튼을 제공합니다.

## 개발

package-lock.json을 사용하는 npm 프로젝트입니다. npm run install:ci, npm run dev, npm run build를 사용합니다. D1 변경은 db/schema.ts 수정 후 npm run db:generate로 migration을 생성합니다. 배포는 Sites 스킬을 따릅니다. .dev.vars와 .tools는 로컬 전용이며 Git·배포 아카이브에 포함하지 않습니다.

APP_ENCRYPTION_KEY: 서버에만 설정하는 32바이트 base64 암호화 키. 기존 데이터가 있는 상태에서 무계획하게 교체하면 기존 API 키를 복호화하지 못합니다. SCHEDULER_STATUS: 예약 상태 표시 문구.

## 공급자 문서

- [OpenAI 웹 검색](https://developers.openai.com/api/docs/guides/tools-web-search)
- [Claude 웹 검색](https://platform.claude.com/docs/en/agents-and-tools/tool-use/web-search-tool)
- [Gemini 검색 기반 답변](https://ai.google.dev/gemini-api/docs/google-search)
